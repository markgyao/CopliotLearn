# FastAPI and ASGI server
fastapi==0.103.1
uvicorn[standard]==0.23.2  # ASGI server with recommended extras

# SQLAlchemy and database interaction
SQLAlchemy==1.4.41  # Compatible with databases==0.6.2
databases==0.6.2    # Async support for SQLAlchemy
mysqlclient==2.2.5  # MariaDB/MySQL driver for SQLAlchemy

# Authentication and security
python-jose==3.3.0  # For handling JWT tokens
passlib[bcrypt]==1.7.4  # For password hashing with bcrypt

# Environment variable management
python-dotenv==1.0.0  # For reading .env files

# Database migrations (if using Alembic)
alembic==1.11.1

# Data validation and application settings
pydantic==2.4.2  # Pydantic version 2+ (used in FastAPI for request/response validation)
pydantic-settings==2.0.3  # For managing application settings

# HTML templating (if serving HTML from FastAPI)
jinja2==3.1.2

# Process management (optional)
gunicorn==21.2.0  # For running FastAPI in production

# Testing and HTTP clients
pytest==7.4.2  # For unit testing
pytest-asyncio==0.21.0  # For testing asynchronous functions
httpx==0.24.1  # For making HTTP requests in tests

# Static analysis and linting (optional but recommended)
flake8==6.1.0  # Linting and code style enforcement
black==23.9.1  # Code formatter
isort==5.12.0  # Import sorting
mypy==1.5.1    # Type checking

python-multipart